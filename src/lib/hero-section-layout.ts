/**
 * Shared viewport hero shell aligned with the home page hero:
 * full-height band under the fixed header (-mt-[100px]), responsive padding,
 * `svh` on small screens and locked viewport height on `md+`.
 */
export const HERO_SECTION_LAYOUT =
  "relative py-28 sm:py-36 md:py-40 flex items-center justify-center overflow-hidden -mt-[100px] min-h-[calc(100svh+80px)] md:h-[calc(100vh+120px)] md:min-h-0";
